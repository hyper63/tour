{
  "name": "demo",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "license": "MIT",
  "dependencies": {
    "hyper-connect": "^0.1.8"
  },
  "scripts": {
    "play": "npx nodemon -r dotenv/config index.js -q -x 'clear && node'"
  },
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.15"
  }
}